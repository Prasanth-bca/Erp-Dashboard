git config --global user.name "Prasanth-bca"
git config --global user.email "prasanth.e390@gmail.com"

git remote add origin https://github.com/Prasanth-bca/Erp-Dashboard.git
git branch -M main   # rename default branch to main (if not already)
git push -u origin main
